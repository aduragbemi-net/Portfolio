[phases.setup]
nixPkgs = ["python310"]

[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "python manage.py collectstatic --noinput"
]

[start]
cmd = "python manage.py migrate && gunicorn feedback_project.wsgi --log-file -"
